# Contact Diary
- This is a Django project of a user interface that performs CRUD operations into a PostgreSQL database.
- It enables a user to enter information into the application which is then stored in a database and displayed on an Excel Sheet for recording purposes.
